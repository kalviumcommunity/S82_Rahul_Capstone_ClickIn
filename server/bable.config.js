export default {
  presets: [
    '@babel/preset-env', // Transpile ES6+ to compatible JS
    '@babel/preset-react'  // If you're using React, add this
  ],
  plugins: [
    // Include any necessary Babel plugins
  ]
};
